import { describe, it, expect } from 'vitest';
import PromptBar from './PromptBar.vue';

describe('Studio Components (src/components/studio)', () => {
  it('should export PromptBar component properly', () => {
    expect(PromptBar).toBeDefined();
  });
});
